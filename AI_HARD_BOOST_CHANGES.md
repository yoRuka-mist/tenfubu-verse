# HARD AI強化 変更内容

## 概要
HARD AIの探索機能を強化し、状況に応じた動的な探索制限と優先度調整を実装しました。

## 実装日
2026-02-02

## 変更ファイル
- `game/src/ai/aiLookahead.ts`

## 主な変更内容

### 1. 動的探索制限の実装
**目的**: 状況に応じて探索深度/ノード数/時間を調整し、効率的な探索を実現

**実装内容**:
- `calculateDynamicSearchLimits()`関数を追加
- プレイヤーHPが低い場合（10以下）: 探索深度+2、ノード数×1.5、時間×1.5
- 手札が多い場合（7枚以上）: ノード数×1.3
- 盤面が複雑な場合（双方合計8体以上）: 探索深度-1、時間×0.8（時間節約）
- リソースが豊富な場合（PP 7以上）: 探索深度+1

**効果**: 
- リーサルに近い状況ではより深く探索
- 複雑な盤面では時間を節約
- リソースが豊富な場合はより深く探索

### 2. アクション優先度の動的化
**目的**: 状況に応じてアクションの優先度を調整し、より適切な行動を選択

**実装内容**:
- `enumerateActions()`関数に動的優先度調整を追加
- リーサルに近い場合（HP 15以下）: リーダー攻撃・リーダーダメージスペルに+50優先度
- 守護が複数いる場合（2体以上）: 守護突破アクションに+30優先度

**効果**:
- リーサルに近い状況では積極的にリーダー攻撃
- 守護が複数いる場合は優先的に処理

### 3. 早期終了の強化
**目的**: より積極的な枝刈りにより、探索効率を向上

**実装内容**:
- `shouldPrune()`関数を強化
- 深度3以上で、ダメージがHP+5未満の場合は枝刈り
- リソース不足（PP 2以下、EP/SEP 0）かつダメージがHP+3未満の場合は枝刈り
- `findLethalPath()`に早期終了条件を追加: 最善パスが見つかっていて、現在のパスがそれより長い場合は枝刈り

**効果**:
- 無駄な探索を削減し、探索時間を短縮
- 最善パスが見つかった場合の早期終了により、探索効率が向上

### 4. 関数シグネチャの更新
**変更内容**:
- `createSearchState()`: `maxDepth`パラメータを追加（デフォルト: 6）
- `findLethalPath()`: `maxNodes`と`timeLimitMs`パラメータを追加（動的制限に対応）
- `shouldPrune()`: `currentDepth`パラメータを追加（深度に応じた枝刈り）

**後方互換性**: 
- 既存の呼び出しはデフォルト値により動作を維持
- `planOptimalSequence()`と`tryFindLethalWithLookahead()`で動的制限を使用

## 期待される効果

1. **探索精度の向上**: 状況に応じた動的制限により、重要な局面でより深く探索
2. **探索効率の向上**: 早期終了と枝刈りの強化により、無駄な探索を削減
3. **行動選択の最適化**: 動的優先度調整により、より適切な行動を選択

## 既存動作への影響

- **互換性**: 既存の動作は維持（デフォルト値により従来の動作を継承）
- **パフォーマンス**: 動的制限により、状況に応じて探索時間が調整される
- **動作確認**: linterエラーなし、既存の関数シグネチャとの互換性を維持

## テスト推奨事項

1. HARD AIでのリーサル判定が正常に動作するか確認
2. プレイヤーHPが低い状況での探索が深くなっているか確認
3. 複雑な盤面での探索時間が適切に制限されているか確認
4. 守護が複数いる場合の優先度調整が機能しているか確認
