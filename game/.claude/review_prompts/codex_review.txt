【サイクル2】v1.51変更のレビュー（Codex指摘修正後）

## 前回の指摘と対応

### 指摘1: バフフォロワーがプレイできなくなる問題
**対応済**: バフ後回しルールをSPELLのみに適用
```typescript
if (shouldWaitForFollowers && card.type === 'SPELL') {
    score += 30; // スペルのみ後回し
}
```

### 指摘2: targetTypeチェック不足
**対応済**: RANDOM_DESTROY/AOE_DAMAGE/RANDOM_DAMAGE_BY_TURNにtargetTypeチェック追加
```typescript
// RANDOM_DESTROY: 相手対象のみ
const targetsOpponent = !eff.targetType || eff.targetType === 'OPPONENT';
if (eff.type === 'RANDOM_DESTROY' && targetsOpponent) { ... }

// AOE_DAMAGE: 敵を含むtargetTypeのみ
const aoeTargetsEnemy = !eff.targetType ||
    eff.targetType === 'ALL_FOLLOWERS' ||
    eff.targetType === 'ALL_ENEMY_FOLLOWERS';
if (eff.type === 'AOE_DAMAGE' && (eff.value || 0) >= 3 && aoeTargetsEnemy) { ... }
```

## レビュー観点
1. 修正が適切に実装されているか
2. 新たなバグや問題がないか
3. 見落としているエッジケース

問題がなければ「LGTM」と回答してください。
