# 最新の修正 (ユキの突進復帰)

## 1. ユキの能力再修正 ✅
- **指示**: ユキの効果を突進に戻す
- **修正**: 
  - カード定義(`c_yuki`)の説明文と`passiveAbilities`を修正
  - `passiveAbilities`: `['STEALTH']` → `['RUSH']`
  - `description`: `[隠密]` → `[突進]`
- **ファイル**: `engine.ts`

## 補足
- Step 266等で実装した「隠密(STEALTH)は守護(WARD)を無視する」ロジック自体は正しい仕様として**保持**しています。現在ユキは隠密を持ちませんが、他の隠密カード（もしあれば）には適用されます。

## 前回の修正内容（通信対戦機能）
- [x] 切断ハンドリングの実装 (NetworkAdapter onConnectionCloseの対応)
